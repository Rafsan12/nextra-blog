# JS Array

# জাভাস্ক্রিপ্ট অ্যারে: সম্পূর্ণ গাইড (বাংলায়)

## 🔰 পরিচিতি

জাভাস্ক্রিপ্টে অ্যারে হচ্ছে একটি বিশেষ অবজেক্ট যা একাধিক মানকে একটি একক ভেরিয়েবলে সংরক্ষণ করতে দেয়। অ্যারে ব্যবহার করে সহজেই ডেটা ম্যানিপুলেট, অ্যাক্সেস এবং সাজানো যায়।

---

## 📦 জাভাস্ক্রিপ্টে অ্যারে কী?

অ্যারে হলো একটি অর্ডার্ড লিস্ট, যেখানে মান (elements) ইনডেক্স (index) অনুযায়ী সংরক্ষিত থাকে। ইনডেক্স ০ থেকে শুরু হয়।

```
let fruits = ['Apple', 'Banana', 'Mango'];

```

---

## 🛠️ কিভাবে একটি অ্যারে তৈরি করবেন?

```
let arr1 = [1, 2, 3]; // লিটারেল সিনট্যাক্স
let arr2 = new Array(4, 5, 6); // কনস্ট্রাক্টর সিনট্যাক্স

```

> ✅ ব্যবহার করবেন যখন: আপনি জানেন যে অ্যারেতে ঠিক কী থাকবে এবং সরাসরি মান দিয়ে তৈরি করছেন।

---

## 🔍 অ্যারে থেকে উপাদান বের করা

```
let item = arr1[1]; // দ্বিতীয় উপাদান (2)

```

---

## ➕ অ্যারেতে উপাদান যোগ করা

```
arr1.push(4);     // শেষে যোগ (mutable)
arr1.unshift(0);  // শুরুতে যোগ (mutable)

```

> ✅ ব্যবহার করবেন যখন: নতুন আইটেম শুরুতে বা শেষে অ্যাড করতে চান।

---

## ➖ অ্যারে থেকে উপাদান অপসারণ

```
arr1.pop();      // শেষ উপাদান সরায় (mutable)
arr1.shift();    // প্রথম উপাদান সরায় (mutable)

```

---

## 🧬 কপি ও ক্লোন

```
let clone = [...arr1]; // spread operator (immutable)
let sliceClone = arr1.slice(); // (immutable)

```

> ✅ ব্যবহার করবেন যখন: মূল অ্যারেকে পরিবর্তন না করে নতুন অ্যারে তৈরি করতে চান।

---

## ❓ কিভাবে যাচাই করবেন কোনো মান অ্যারে কিনা?

```
Array.isArray(arr1); // true

```

---

## 🧹 ডেস্ট্রাকচারিং

```
let [a, b] = [10, 20];

```

### ✅ ডিফল্ট মান:

```
let [x = 5, y = 10] = [];

```

### 🚫 মান স্কিপ করা:

```
let [, second] = [1, 2, 3];

```

### 🔁 নেস্টেড অ্যারে:

```
let [a, [b, c]] = [1, [2, 3]];

```

### 🌊 রেস্ট প্যারামিটার:

```
let [first, ...rest] = [1, 2, 3, 4];

```

### 🔄 স্প্রেড অপারেটর:

```
let newArr = [...arr1, 5, 6];

```

---

# Rest vs Spread

## Rest Operator (`...rest`)

**Rest Operator** ব্যবহার করা হয় যখন আমরা function এর parameters বা object/array থেকে অবশিষ্ট মানগুলো সংগ্রহ করতে চাই।

### Rest in Function Parameters

```jsx
function sum(a, b, ...rest) {
  console.log(a); // প্রথম প্যারামিটার
  console.log(b); // দ্বিতীয় প্যারামিটার
  console.log(rest); // বাকি সব প্যারামিটার array আকারে
}

sum(1, 2, 3, 4, 5);
```

➡️ Output:

```
1
2
[3, 4, 5]
```

### Rest in Array Destructuring

```jsx
const numbers = [1, 2, 3, 4, 5];

const [first, second, ...rest] = numbers;

console.log(first); // 1
console.log(second); // 2
console.log(rest); // [3, 4, 5]
```

---

## Spread Operator (`...spread`)

**Spread Operator** ব্যবহার করা হয় যখন আমরা একটি array বা object-এর সব elements/alements/alements/alements কে অন্য container এর মধ্যে ছড়িয়ে দিতে চাই।

### Spread in Arrays

```jsx
const arr1 = [1, 2, 3];
const arr2 = [4, 5];

const combined = [...arr1, ...arr2];

console.log(combined); // [1, 2, 3, 4, 5]
```

### Spread to Clone Array

```jsx
const numbers = [1, 2, 3];
const clone = [...numbers];
console.log(clone); // [1, 2, 3]
```

---

## Rest vs Spread পার্থক্য চার্ট

| Rest (`...`)                                                                | Spread (`...`)                             |
| :-------------------------------------------------------------------------- | :----------------------------------------- |
| মান গুলো সংগ্রহ করে                                                         | মান গুলো ছড়িয়ে দেয়                      |
| Function parameters, object destructuring, array destructuring এ ব্যবহার হয় | Array/object merge/cloning করতে ব্যবহার হয় |
| অনেকগুলো value কে একত্রিত করে                                               | এক value কে অনেক জায়গায় ছড়িয়ে দেয়     |

---

## মনে রাখার Shortcut

- **Rest = Collect** (সংগ্রহ করা)
- **Spread = Distribute** (বিতরণ করা)

---

## 🔄 মান অদলবদল

```
let a = 1, b = 2;
[a, b] = [b, a];

```

---

## 🔗 অ্যারে মার্জ

```
let merged = [...arr1, ...arr2];

```

---

## 📏 length প্রপার্টি

```
let len = arr1.length;

```

---

## 🧰 সাধারণ অ্যারে মেথডস

| মেথড           | কার্য                 | মিউটেবল? | কখন ব্যবহার করবেন               |
| -------------- | --------------------- | -------- | ------------------------------- |
| `push()`       | শেষে যোগ              | ✅ হ্যাঁ | উপাদান শেষে যোগ করতে            |
| `pop()`        | শেষে থেকে সরানো       | ✅ হ্যাঁ | শেষ উপাদান সরাতে                |
| `shift()`      | শুরু থেকে সরানো       | ✅ হ্যাঁ | প্রথম উপাদান সরাতে              |
| `unshift()`    | শুরুতে যোগ            | ✅ হ্যাঁ | শুরুতে মান যোগ করতে             |
| `concat()`     | একত্রিত               | ❌ না    | দুটি অ্যারে মার্জ করতে          |
| `join()`       | স্ট্রিংয়ে রূপান্তর   | ❌ না    | অ্যারে মান স্ট্রিং বানাতে       |
| `fill()`       | নির্দিষ্ট মানে পূর্ণ  | ✅ হ্যাঁ | সব আইটেম একই মানে দিতে          |
| `includes()`   | মান আছে কি না         | ❌ না    | উপাদান আছে কিনা দেখতে           |
| `indexOf()`    | প্রথম ইনডেক্স খোঁজে   | ❌ না    | মানের অবস্থান জানতে             |
| `reverse()`    | উল্টে দেয়            | ✅ হ্যাঁ | ইনপ্লেস উল্টাতে                 |
| `sort()`       | সাজায়                | ✅ হ্যাঁ | উপাদান sort করতে                |
| `splice()`     | সরানো/যোগ             | ✅ হ্যাঁ | নির্দিষ্ট জায়গায় অপারেশন করতে |
| `slice()`      | কপি করে               | ❌ না    | নির্দিষ্ট অংশ পেতে              |
| `at()`         | ইনডেক্স অনুসারে       | ❌ না    | নেগেটিভ ইনডেক্সেও কাজ করে       |
| `copyWithin()` | কপি করে ইনপ্লেস বসায় | ✅ হ্যাঁ | ডেটা রিপ্লেস করতে               |
| `flat()`       | nested ফ্ল্যাট করে    | ❌ না    | নেস্টেড অ্যারে সমতল করতে        |

---

## 🧱 স্ট্যাটিক মেথডস

```
Array.isArray(), Array.from(), Array.of(), Array.fromAsync()

```

---

## 🔄 ইটারেটর মেথডস

| মেথড              | কার্য                     | মিউটেবল? |
| ----------------- | ------------------------- | -------- |
| `forEach()`       | লুপ চালায়                | ❌ না    |
| `map()`           | মান রূপান্তর করে          | ❌ না    |
| `filter()`        | শর্তমতো ফিল্টার           | ❌ না    |
| `reduce()`        | একটি মানে রিডিউস          | ❌ না    |
| `reduceRight()`   | ডানদিক থেকে রিডিউস        | ❌ না    |
| `some()`          | শর্ত মিললে true           | ❌ না    |
| `every()`         | সব মান শর্ত পূরণ করে কিনা | ❌ না    |
| `find()`          | প্রথম মিল পাওয়া মান      | ❌ না    |
| `findIndex()`     | প্রথম ইনডেক্স             | ❌ না    |
| `findLast()`      | শেষ মিল                   | ❌ না    |
| `findLastIndex()` | শেষ ইনডেক্স               | ❌ না    |
| `entries()`       | key-value pair            | ❌ না    |
| `values()`        | শুধুমাত্র মান             | ❌ না    |

---

## 🧊 Immutability (অপরিবর্তনশীলতা)

| মেথড           | কার্য                                     | মিউটেবল? |
| -------------- | ----------------------------------------- | -------- |
| `toReversed()` | উল্টো করে নতুন অ্যারে                     | ❌ না    |
| `toSorted()`   | sort করে নতুন অ্যারে                      | ❌ না    |
| `toSpliced()`  | splice এর immutable ভার্সন                | ❌ না    |
| `with()`       | নির্দিষ্ট ইনডেক্সে মান বসিয়ে নতুন অ্যারে | ❌ না    |

---

## ✅ উপসংহার

- **Mutable Methods**: `push`, `pop`, `shift`, `unshift`, `splice`, `sort`, `reverse`, `copyWithin`, `fill`
- **Immutable Methods**: `map`, `filter`, `slice`, `concat`, `toReversed`, `toSorted`, `toSpliced`, `with`, `flat`

## Advance

# 📚 Array Length

## ✨ সংজ্ঞা (Definition)

**Array Length** হলো একটি অ্যারেয়ে মোট কয়টি উপাদান আছে, তা বোঝায়।  
JavaScript-এ প্রতিটি অ্যারের সাথে একটি `length` প্রপার্টি থাকে, যা সংখ্যা আকারে অ্যারেটির দৈর্ইঘ্য (element সংখ্যা) প্রকাশ করে।

---

## 🛠️ কিভাবে কাজ করে?

- `array.length` ব্যবহার করে এটি অ্যারের উপাদানগুলোর সংখ্যা ফিরে।
- অ্যারের ইনডেক্স 0 থেকে শুরু হয়, কিন্তু length ইনডেক্স নয়, উপাদানের মোট সংখ্যা।
- অ্যারে আইটেম যোগ করলে বা সরালে, `length` আপনে-আপনিক অপডেট হয়।

---

## ✅ সহজ উদাহরণ:

```javascript
const colors = ["লাল", "নীল", "সবুজ"];
console.log(colors.length);
// আউটপুট: 3
```

---

## 🔥 কিভাবে length দিয়ে কাজ করা হয়?

### ❃ সর্বশেষ উপাদান বের করা:

```javascript
const fruits = ["আপেল", "কমলা", "কলা"];
const lastFruit = fruits[fruits.length - 1];
console.log(lastFruit);
// আউটপুট: "কলা"
```

---

### ❃ লুপ চালানো:

```javascript
const numbers = [10, 20, 30, 40];

for (let i = 0; i < numbers.length; i++) {
  console.log(numbers[i]);
}
```

---

## 📌 গুরুত্ব বিষয়গুলো:

### 1. অ্যারে খালি থাকলে:

```javascript
const emptyArray = [];
console.log(emptyArray.length);
// আউটপুট: 0
```

### 2. length ম্যানুয়ালি পরিবর্তন করলে:

```javascript
const nums = [1, 2, 3, 4, 5];
nums.length = 2;
console.log(nums);
// আউটপুট: [1, 2]
```

### 3. length বাড়ালেয়ো কাজ করে:

```javascript
const nums = [1, 2];
nums.length = 5;
console.log(nums);
// আউটপুট: [1, 2, <3 empty items>]
```

---

## 🧐 Interview Tips:

| প্রশ্ন                  | উত্তর                     |
| :---------------------- | :------------------------ |
| অ্যারের শেষ উপাদান পেতে | `array[array.length - 1]` |
| যদি length কমানো?       | অতিরিক্ত আইটেম কেটে যায়   |
| যদি length বাড়ানো?      | নতুন আইটেম empty থাকবে    |

---

## 🚀 কিছু বাস্তব জীবনের ব্যবহারে উদাহরণ:

| কাজ                  | কোড                           | উদ্দেশ্য              |
| :------------------- | :---------------------------- | :-------------------- |
| অ্যারে খালি কিনা চেক | `if (array.length === 0)`     | কোনো ডেটা আছে কি চেক  |
| সব উপাদান দেখানো     | `for...of` বা `for` লুপ দিয়ে | অ্যারের সব আইটেম দেখা |

---

# 🔥 উপসংহার (Summary):

- `length` অ্যারের মোট উপাদান সংখ্যা প্রকাশ করে।
- যদি আইটেম যোগ/সরালে, `length` বদলে পরিবর্তিত হয়।
- `length` পরিবর্তন করে আপনার একটি অ্যারের আকার ছোট বা বড়ানো যায়।

---

# 📆 একটি প্রশ্ন: কতটো অ্যারে এড করা যায়?

# JavaScript-এ অ্যারের সর্বোচ্চ লেংথ

JavaScript-এ অ্যারের সর্বোচ্চ length হচ্ছে:

```
2³² - 1 = 4,294,967,295
```

মানে: প্রায় **4.2 বিলিয়ন** আইটেম রাখা সম্ভব একটি অ্যারেতে!

---

## 📜 একটু ডিটেইল করে বলি:

- JavaScript-এ অ্যারের length একটি **unsigned 32-bit integer** হিসেবে সংরক্ষিত হয়।
- তাই সর্বোচ্চ ভ্যালু হতে পারে:

```
2^32 - 1 = 4294967295
```

- এর বেশি বড় length দিলে **RangeError** দিবে।

---

## 🔥 Example:

```javascript
const hugeArray = new Array(4294967295);
console.log(hugeArray.length);
// আউটপুট: 4294967295
```

---

❗ মনে রাখতে হবে:

- এত বড় অ্যারে তৈরি করা বাস্তবে প্রায় অসম্ভব, কারণ **RAM (মেমোরি) লিমিটেশন** আছে।
- সাধারণ কম্পিউটার হয়তো **কয়েক লাখ বা কয়েক মিলিয়ন**ের বেশি আইটেম হ্যান্ডেল করতে পারে।
- এত বড় অ্যারে করলে **performance crash** বা ব্রাউজার freeze\*\* করতে পারে।

---

## 🎯 সংক্ষেপে:

| বিষয়                  | ব্যাখ্যা                                    |
| --------------------- | ------------------------------------------- |
| সর্বোচ্চ Array Length | 4,294,967,295 (2³² - 1)                     |
| কিভাবে আসে            | 32-bit unsigned integer                     |
| সমস্যা                | মেমোরি সমস্যা বা পারফর্মেন্স ইস্যু হতে পারে |
